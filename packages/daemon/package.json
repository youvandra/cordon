{
  "name": "@cordon/daemon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "abi": "node scripts/emit-abi.mjs",
    "build": "npm run abi && tsc --noEmit",
    "test": "npm run abi && node --test \"test/*.test.ts\"",
    "start": "node src/main.ts",
    "init": "node src/init-cli.ts"
  },
  "dependencies": {
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "bin": {
    "cordon-init": "src/init-cli.ts"
  }
}
